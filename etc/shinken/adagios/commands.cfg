define command {
    command_name    check_dig_service
    command_line    $PLUGINSDIR$/check_dig -H $ARG1$ -l $ARG2$
}

define command {
    command_name    check_http2
    command_line    $PLUGINSDIR$/check_http2 -U $ARG1$
    }

define command {
    command_name    check_quebecrencontrescom
    command_line    $PLUGINSDIR$/check_quebecrencontrescom
    }

define command {
    command_name    check_reseaucontactcom
    command_line    $PLUGINSDIR$/check_reseaucontactcom
    }

define command {
    command_name    check_stm_metro
    command_line    $PLUGINSDIR$/check_stm_metro -w $ARG1$ -c $ARG2$
    }

define command {
    command_name    check_emergency_occupation
    command_line    $PLUGINSDIR$/check_emergency_occupation -w 80 -c 100 -U '$ARG1$' -f '$ARG2$' -o '$ARG3$'
    }

define command {
    command_name    check_dummy
    command_line    $PLUGINSDIR$/check_dummy $ARG1$ $ARGG2$
    }
