# This file contains tokens, for data sources which require one.
# If you don't want to sign-up to these services, leave this empty,
# and the configuration for these particular services won't be generated.

# Don't add your tokens here, but instead copy this file to `tokens.py`,
# in order to keep your git status clean.

TOKENS = {
    'AMT': '',
}
